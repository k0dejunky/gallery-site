RewriteEngine On

# Upload limits are now set in the PHP-FPM pool (www.conf) and apply to
# all sizes; removing php_value here since FPM ignores/halts on them.

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
